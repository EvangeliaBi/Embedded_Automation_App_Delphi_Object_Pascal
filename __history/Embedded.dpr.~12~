program Embedded;

uses
  Vcl.Forms,
  Main in 'Main.pas' {frmMain},
  EmbeddedCPU in 'EmbeddedCPU.pas',
  Scheduler in 'Scheduler.pas',
  GPIO in 'GPIO.pas',
  ADC in 'ADC.pas',
  PWM in 'PWM.pas',
  UART in 'UART.pas',
  InterruptManager in 'InterruptManager.pas',
  PlantSimulation in 'PlantSimulation.pas',
  Logger in 'Logger.pas',
  DigitalInputsForm in 'DigitalInputsForm.pas' {frmDigitalInputs},
  DigitalOutputsForm in 'DigitalOutputsForm.pas' {frmDigitalOutputs},
  AnalogInputsForm in 'AnalogInputsForm.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TfrmMain, frmMain);
  Application.CreateForm(TfrmDigitalInputs, frmDigitalInputs);
  Application.CreateForm(TfrmDigitalOutputs, frmDigitalOutputs);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
